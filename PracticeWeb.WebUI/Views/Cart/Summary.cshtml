@model Practice.Domain.Entities.Cart

<div class="navbar-text mr-sm-2">
    <b>購物車:</b>
    @Model.Lines.Sum(x => x.Quantity) 品項,
    @Model.ComputeTotalValue().ToString("c0")
</div>

<div class="navbar-right">
    @Html.ActionLink("結帳", "Index", "Cart",
    new { returnUrl = Request.Url.PathAndQuery },
    new { @class = "btn btn-secondary my-2 my-sm-0" })
</div>